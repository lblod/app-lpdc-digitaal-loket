# Add user to controle environment

- run `htpasswd -n <user-email>` on local machine
- copy and past in app-lpdc-digitaal-loket/config/basic-auth/.httpasswd on remote machine
- securely send password to the user
